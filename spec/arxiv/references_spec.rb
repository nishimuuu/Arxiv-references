require 'spec_helper'

describe Arxiv::References do
  it 'has a version number' do
    expect(Arxiv::References::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
